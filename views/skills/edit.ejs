<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h1>Edit Player Info</h1>
    <form action="/skills/<%= skill._id %>?_method=PUT " method="POST" autocomplete="off">
        <input value="<%= skill.name %>" type="text" name="name">
        <input value="<%= skill.position %>" type="text" name="position">
        <input value="<%= skill.jersey %>" type="text" name="jersey">
        <input type="checkbox" <% skill.start ? 'checked' : '' %> name="start">Starting?
        <button type="submit">Update</button>
    </form>
    <a href="/skills/<%= skill._id %> ">Cancel</a>
</body>
</html>